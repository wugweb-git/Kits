# Components

Canonical runtime import path: `src/components/design-system/components.ts` -> `src/components/ui/index.ts`.

Legacy components remain as temporary adapters under `src/components/ui/legacy-adapters.ts` during migration.
