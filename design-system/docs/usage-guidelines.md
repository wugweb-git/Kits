# Usage Guidelines

- Import components from the design-system bridge only.
- Prefer tokenized styles (`var(--token)`) and semantic token names.
- Avoid direct imports from `src/components/wugweb` and `src/imports`.
