/**
 * Canonical component bridge for docs/runtime imports.
 * This bridge points to the unified ui tree and re-exports legacy
 * adapters to preserve backwards compatibility during migration.
 */
export * from '../ui';
export * from '../ui/legacy-adapters';
